# PROJECT_PLAN.md — FDM Platform Roadmap

Sessions are executed in order, one Claude Code conversation each. A session is done only when its acceptance criteria pass and it is committed. Tick the box when done.

Reference: `CLAUDE.md` (rules), `docs/implementation-plan.md` (architecture), `docs/uiux-spec.md` (design system + screens).

---

## Phase 0 — Scaffold & Foundation

- [x] **0.1 Monorepo scaffold.** Repo layout per CLAUDE.md, FastAPI skeleton with `/api/health`, Vite+Vue3+TS+frappe-ui+Tailwind app shell (dark default), docker-compose.dev.yml (Postgres+Redis), Makefile, .env.example, README.
  *Accept:* `make dev` boots API + SPA; health returns `{"status":"ok"}`; browser shows dark app shell.
- [x] **0.2 Config, DB, app skeleton.** Pydantic Settings, SQLAlchemy 2 + Alembic wired (empty first migration), structured JSON logging, global error handlers, CORS; frontend router + sidebar layout with all nav groups (empty placeholder pages), light/dark toggle.
  *Accept:* `alembic upgrade head` runs; all sidebar routes render placeholders; toggle persists.
- [x] **0.3 Core component library + design tokens.** Tailwind tokens from CLAUDE.md; build StatusDot, StatusBadge, EnvironmentBadge, KPICard, DataTable (sort/filter/density/sticky header/row actions), ConfirmModal (standard + destructive type-to-confirm), Wizard (stepper), EmptyState, Toast, CopyField, JobTimeline (static), LogViewer (static), Sparkline. Demo everything on a `/styleguide` route.
  *Accept:* `/styleguide` renders every component in both themes; destructive modal blocks until exact name typed.
- [x] **0.4 Dev & test workflow docs.** `docs/dev-setup.md`: running control plane against Hyper-V Ubuntu VMs, SSH reachability, snapshot-before-destructive-test workflow (Checkpoint-VM PowerShell snippet), throwaway-VM policy.
  *Accept:* doc is followable start-to-finish by a non-technical user.

## Phase 1 — MVP (vertical slice on one Hyper-V VM)

- [x] **1.1 Auth & RBAC.** User/Role/ApiToken models + migration; argon2; JWT httpOnly cookies + CSRF; login/logout/me endpoints; RBAC dependency; seed admin CLI; login page; route guards; user menu.
  *Accept:* login works; Read-only user gets 403 on a mutating endpoint; wrong password rate-limited.
- [x] **1.2 Server registry & SSH test.** Server + SSHCredential models; Fernet SecretsService; AsyncSSH service with pooling + known-host pinning; Add Server sheet wizard (identity → auth: paste/upload/generate keypair with copy-pubkey instructions → live streamed Test Connection: SSH ✓, sudo ✓, OS ✓, detected tools table); servers list + detail Overview tab.
  *Accept:* register the Hyper-V VM; test connection streams checks and stores results; private key unreadable in DB (Fernet token).
- [x] **1.3 Job engine core.** CommandJob/CommandStep/LogEntry models; RQ queues high/default/low; JobRunner + ctx.step; command template registry with validators + secret masking; Redis job locking (409 on conflict); 202+job_id pattern; retry (3×, idempotent only); jobs list/detail API; 4h timeout + graceful worker shutdown configured.
  *Accept:* a demo `echo`-over-SSH job runs through the full lifecycle; concurrent duplicate returns 409; unit tests prove injection-unsafe input is rejected.
- [x] **1.4 Live logs.** Worker persists LogEntry batches + publishes to pub/sub; SSE endpoint with replay + tail + heartbeat; Jobs page (live table) + Job detail (step timeline left, live LogViewer right, sanitized params, "Show exact command" expander, role-gated Cancel/Retry); persistent bottom-right job tray.
  *Accept:* watching a 60s multi-step demo job, logs stream live, steps tick, tray shows progress, refresh mid-job resumes cleanly.
  *Carry-in (DOO-94 #2 → owned by DOO-96):* on idempotent auto-retry, each attempt restarts `CommandStep.order` at 1, so a 3-step action retried writes orders 1,2,3,1,2,3 — the step-timeline UI can't tell attempts apart. DOO-96 adds a 1-based `CommandStep.attempt` discriminator (Alembic migration + `StepOut.attempt`); build the timeline to group by `attempt` then render each attempt's steps in `order`.
- [ ] **1.5 Browser SSH terminal.** POST /terminal/sessions issues short-lived ticket; WS bridges xterm.js ↔ AsyncSSH PTY; resize handling; multiple tabs; context bar; idle timeout; Developer+ only; audit row per session.
  *Accept:* open terminal to the VM, run `htop`, resize works, idle disconnect fires, session appears in audit log.
- [x] **1.6 Bench discovery & list.** Discovery job: scan for benches, parse `sites/common_site_config.json` (ports), `bench version`; Bench model + list UI grouped by server with version chips and port map.
  *Accept:* existing v15/v16 benches on the VM appear with correct versions and ports.
- [x] **1.7 Bench create.** Wizard (server → version radio cards showing the matrix → streamed pre-flight job: uv/node/mariadb/wkhtmltopdf present, ports free, disk → name/path → review with exact commands → job). Encode version matrix + gotchas #1–#6 from CLAUDE.md.
  *Accept:* create a fresh v16 bench on the VM end-to-end from the UI with live logs; pre-flight blocks when uv is missing.
- [x] **1.8 Site list & create.** Site model; create wizard (bench → validated name → generated admin password → review → job) using gotcha #4 flags; handles dev-bench Redis gotcha #3 (start 11000/13000, stop after); site list with health dot + scheduler state; site detail Overview with quick actions (open ↗, maintenance toggle, scheduler toggle).
  *Accept:* create `test1.localhost` on the v16 bench from UI; site loads in browser; no interactive prompt ever hangs the job.
- [x] **1.9 App install (ERPNext / GitHub).** AppSource + InstalledApp models; add-source flow (URL → branch fetch → compatibility warning); `bench get-app --branch` + `install-app` jobs; private repo deploy-key support; installed-apps table on site detail.
  *Accept:* install ERPNext on the test site from UI; install one custom GitHub app by URL+branch.
- [x] **1.10 Maintenance actions.** Migrate / clear-cache / clear-website-cache / build / restart as jobs from bench + site detail, each behind ConfirmModal; bulk "migrate all sites on bench". Added `bench.update` (high queue, downtime-warning confirm, auto db-only safety-backup pre-step) and mode-aware `bench.restart` (prod `sudo supervisorctl restart <group>:*` via the ratified sudoers allowlist; dev = informative failure). Site ops wrap the dev-bench Redis dance (gotcha #3); no model change (no migration). Landed each action on job detail.
  *Accept:* each action produces a job with correct logs and audit rows.
- [ ] **1.11 Backup & restore.** Backup model; `bench backup --with-files` job parsing the 3 artifacts + config, sha256 checksums, size; Backups page with filters; guided Restore flow (pick/validate → target same/new-site/other-bench with compatibility check → red review + type-to-confirm → auto pre-restore backup → restore job → auto encryption_key copy + migrate → success card). Gotcha #7 enforced.
  *Accept:* backup the seeded site; restore to a NEW site; a known record survives; restoring OVER a site requires typed confirmation and takes a pre-backup first.
- [x] **1.12 Dashboard v1, monitoring, audit, settings.** SSH-polled CPU/RAM/disk + service states (nginx/mariadb/redis/supervisor); Dashboard per uiux-spec (KPI cards incl. Fleet Health, morning-brief line, servers strip, backup 7-day grid, first-run onboarding empty state); Audit Log page (filterable, CSV export); Settings (general incl. product name/logo + TZ, defaults). Landed: `AuditLog` + shared `record_audit` funnel (JobRunner + non-job endpoints, source IP via request ContextVar); monitoring poller (`server.restart_service` via sudoers) + services grid; dashboard aggregation w/ documented Fleet Health formula; white-label settings + logo; `docs/mvp-demo.md`. Migration `a7c3e9f10b22` (up/down/up on PG18); 369 pytest / ruff / vite build clean. Tagged `v0.1.0-mvp`.
  *Accept:* dashboard answers "is everything okay?" in 10 seconds; audit shows every action from sessions 1.1–1.11; MVP DEMO: register server → create bench → create site → install ERPNext → migrate → backup → restore, entirely from the UI.
  *Carry-in (DOO-94 #3):* golden rule 2 wants an `AuditLog` row (incl. **source IP**) per state-changing op; 1.3–1.11 wrote only `CommandJob`. When adding AuditLog here, capture source IP at job-create time (thread the request `X-Forwarded-For`/client host into `JobRunner.create`) going forward, and explicitly accept that 1.3–1.11 history predates it (no back-fill of source IP is possible).

## Phase 2 — Backups at scale, SSL/Domains, Multi-server
2.1 Scheduler (RQ-scheduler) + backup schedules/retention · 2.2 S3-compatible storage + upload/verify/download · 2.3 Backup compliance engine (RPO/RTO policies, dashboard %) · 2.4 Domains & SSL (nginx vhost gen, `nginx -t` + filelock, certbot, DNS check, expiry tracking) · 2.5 Production setup job (`bench setup production` with config pre-backup) · 2.6 Multi-server hardening + per-server dashboards · 2.7 Uptime HTTP checks + response-time charts · 2.8 Command palette (⌘K) + notification center.

- [x] **2.1 Scheduler (rq-scheduler) + backup schedules & retention sweeps.** `Schedule` model + migration `d3f5a1c7e9b2` (target_type/id, action_name, cron|interval, timezone default `Asia/Dubai`, enabled, next_run_at, last_run_at, last_run_job_id, retention params). rq-scheduler process (`app/workers/scheduler.py`, `make scheduler`) registers a single recurring tick; each tick sweeps due schedules and enqueues real `CommandJob`s through the JobRunner (golden rules 2/3) — a scheduled fire is locked, audited and streamed exactly like a hand-launched job. Reuses `site.backup` (1.11); adds `backup.retention_sweep` (dry-run kept/removed log first, `rm -f` only paths under `private/backups/`, **never the newest/only backup**, then delete the rows). CRUD `/api/schedules` (Admin/Developer manage via `schedule:manage`, Operator run-now via the action's own permission, Read-only view) + enable/disable + run-now → job. **Missed-run policy:** the scheduler fires a due schedule once and recomputes `next_run_at` forward from now — a scheduler outage yields one catch-up run, never a backlog stampede; disabling makes it non-due. Schedules page (list-first per B4.12: target, action, cadence, next-run rel+abs-on-hover, last result, enabled toggle, EmptyState); calendar deferred to 3.5. Migration up/down/up on PG18; 30 new pytest / ruff / vite build clean. **Live backup/retention runs gated behind DOO-36 test-target** (mocked-SSH + fake-clock tests cover the logic).
  *Accept:* create a daily backup schedule → it fires as a CommandJob (or run-now) producing a backup + audit row; a retention sweep removes only backups beyond the window (logs kept/removed, keeps the newest); disabling stops future runs.

- [x] **2.3 Backup compliance engine (RPO/retention policy + real dashboard %).** `BackupPolicy` (per-site, unique `site_id`: `rpo_hours` default 24, `retention_days`, `require_offsite`, `require_restore_test` [groundwork], `enabled`), `ComplianceStatus` (per policied site: state compliant/breached/unknown, `last_backup_at`, `breaches[]`, `evaluated_at`), and `ComplianceBreachEvent` (channel-free record 3.1 consumes; `consumed_at` groundwork) + migration `f8b3d1c6a2e9` (single head after `f7a2c9e1b3d4`; up/down/up on PG18). `compliance.evaluate_all` sweeps every **enabled** policy read-only over backup metadata (retention *deletes* stay in 2.1): compliant = newest success backup age ≤ RPO **AND** retention history reaches back (once the site is old enough) **AND** (newest backup offsite if required); persists `ComplianceStatus` and **edge-triggers** one breach event on transition *into* breach (no per-tick spam). Wired into the 2.1 scheduler process as a second recurring sweep (`COMPLIANCE_TICK_SECONDS`, hourly). **Dashboard Backup Compliance % is now real** (`compliant / policied`; 100 when nothing policied) and the Fleet Health **backup component (weight 30) is now driven by it** (was the 1.12 "backed-up in 24h" placeholder). `GET/PUT/DELETE /api/sites/{id}/policy` (`schedule:manage`), `GET /api/compliance` summary (Read-only+), `POST /api/compliance/evaluate` (run now). Frontend: Backups → Policies tab (per-site RPO/retention editors, B4.6), Dashboard compliance card wired real, per-site compliance tick in Sites/Backups. 20 new pytest / ruff / vite build clean. **End-to-end % with real backups gated behind DOO-36 test-target** (evaluator fully covered by seeded-backup + fake-clock tests).
  *Accept:* set 24h RPO → a fresh backup reads compliant; skipping past RPO flags a breach, drops the dashboard %, and writes a `ComplianceBreachEvent` row for 3.1.

- [x] **2.6 Multi-server hardening + per-server dashboards.** SSH **connection-pool limits** in `SSHService`: a per-server `asyncio.Semaphore` (cap `Settings.ssh_max_sessions_per_server`, per-host override `Server.ssh_pool_limit`) wraps every session-opening path (`run`/`stream`/`stream_file`/`read_file`/`write_file`); beyond the cap callers **queue** (wait) rather than fail, and only a slot that never frees within `ssh_session_acquire_timeout_seconds` raises `SessionPoolTimeout` (clean backpressure, never a hang) — the semaphore is keyed on the server id stashed on the pooled connection, so pools are per-server independent and a job never deadlocks waiting on itself. `backup.move_across_servers` job (`MoveBackupAction`, Developer+ `backup:transfer`, locked on the destination site, audited via `JobRunner.create`): streams each artifact of an **already-offsite** backup out of its S3 `StorageTarget` (reusing 2.2 `S3Config`/`build_client` + new `download_object`) straight onto the destination server via `write_file`, computing the sha256 in flight AND re-running `sha256sum` on the landed file — a mismatch on either check refuses the move — then registers the moved copy (`moved_from_backup_id`/`source_server_id` provenance). S3 is the deliberate cross-server transit medium (agentless: no A→B trust). Per-server rollup `server_dashboard()` (capacity/health from the latest monitoring sample, bench/site counts, site up/down from newest uptime probe, 24h job outcomes, backup footprint). API: `GET /api/servers/{id}/dashboard`, `POST /api/backups/{id}/move`. Frontend: Server Overview per-server KPI strip (B4.2); Backups "Move" action + modal (destination bench on another server) + "Moved" chip. Migration `a2b6d4f8c3e1` (single head, chained after main head `dbaa1a7ed7d6` on merge; up/down/up on PG18). 15 new pytest / ruff / vite build clean. **Live 2-server move + real checksum acceptance gated behind DOO-36 test-target** (mocked-S3 + in-memory-executor tests cover the logic).
  *Accept:* with 2 servers registered, cap concurrent sessions and observe queued (not failed) ops under load; move an offsite backup A→B and verify the checksum matches on arrival; each server's dashboard shows its own rollup.

- [x] **2.7 Uptime HTTP checks + response-time charts.** (landed in e5f2eba / ba81464)

- [x] **2.8 Command palette (⌘K) + notification center.** `Notification` + `NotificationPreference` models + migration `f4c6e8a0b2d1` (single head, chains after `e4c7b1a9f2d0`; up/down/up on PG). `NotificationService`: create/dispatch/prune per user; dispatch hooks in `JobRunner._to_terminal` (job.success/failure) and `store_uptime_sample` (uptime.down/restored on state flip); minimal SMTP + signed HMAC-SHA256 webhook senders. `GET /api/notifications` feed + prefs CRUD; `GET /api/search` role-scoped palette endpoint (Read-only: no mutating actions; actions → registered command templates only). Frontend: `CommandPalette.vue` (⌘K / Ctrl+K / `/` — entity+action+nav results, recents, keyboard nav); `NotificationDrawer.vue` (bell + unread badge + feed + per-event channel toggles); `useKeyboardShortcuts` composable (`g d`, `g j`, `t`, `/`); AppTopbar placeholder replaced; App.vue mounts palette + starts notification polling. 22 new pytest / ruff clean / vite build clean. Commit `25ad2f2`.
  *Accept:* ⌘K opens anywhere; finds a site by name; "Backup site…" dispatches as a real CommandJob (navigates to /jobs/{id}); Read-only sees no mutating actions; job failure + uptime flip → bell feed entry; signed webhook fires for subscribed event type.

- [x] **2.5 Production setup (`bench setup production` + config pre-backup + dev→prod toggle).** `bench.setup_production` orchestrator (high queue): detect dev/prod (refuse if already prod) → pre-backup + sha256 the nginx/supervisor config → install a **time-boxed, single-command sudoers drop-in** (the Phase 2.5 sudo decision point — NOT a permanent `bench setup production` grant) → `sudo -n <bench> setup production <user>` → toggle `Bench.is_production` → before→after config diff (`POSTCONFIG_RESULT`) → `sudo -n nginx -t` → **always revoke the drop-in** (finally). Standing privilege is only the fixed `deploy/fdm-elevate` helper (backup/manifest/grant/revoke), so at rest no sudo path to setup production exists (drift-baseline-clean, 6.7). `POST /api/benches/{id}/setup-production`; Bench detail → destructive ConfirmModal (type-the-bench-name + config-pre-backup notice); mode badge already reflects `is_production`. No model change (`is_production` pre-existed) → no migration. 422 pytest (22 new) / ruff / vite build clean. Mechanism + rollback in `docs/production-setup.md`. **Live acceptance (real root bench + sudo) gated behind DOO-36 test-target.** Gates 2.4 (SSL vhosts need production nginx).
  *Accept:* on a dev bench, Set up production; configs captured before; command completes; bench shows prod mode; sites served by nginx/supervisor; job shows before/after diff; rollback documented.

## Phase 3 — Alerts, Safe Updates, Schedules
3.1 AlertRule engine + email/webhook channels · 3.2 Update advisor (release/security detection, changelog preview) · 3.3 Safe update pipeline (clone → staging → verify checklist → promote, rollback path) · 3.4 Restore-test automation (scheduled restore→verify→destroy, "restore-tested" badge) · 3.5 Schedules calendar + maintenance windows (block dangerous jobs).

## Phase 4 — Full-system DR
4.1 restic per server → S3 (config tier: nginx/supervisor/redis/mariadb configs + `dpkg --get-selections`) · 4.2 Weekly system snapshots + retention + `restic check` · 4.3 DR runbook generator · 4.4 Compliance report exports (audit + backup evidence, ISO-friendly).

- [x] **4.4 Compliance/audit report exports (PDF/CSV, ISO-friendly).** `POST /api/compliance-reports/generate` (new `report:generate` permission — Admin + Developer; Read-only 403). Three report types: `access` (AuditLog date-range slice), `backup_evidence` (per-site BackupPolicy + ComplianceStatus join), `access_review` (users × roles point-in-time). CSV (UTF-8 BOM for Excel) + PDF (reportlab A4/landscape, dark header, alternating rows). SHA-256 content-hash of each export recorded in `AuditLog` via `compliance.export_report` action (tamper-evidence). Migration `a2b6d4f8c3e1` (data-only: adds `report:generate` to Developer role permissions JSON). Frontend: `ComplianceReportGenerator.vue` on the Audit page (date-range picker, report-type select, PDF/CSV toggle, generate → download, hash display). `reportlab>=4.2` added to pyproject.toml. 21 new pytest / ruff clean / vite build clean. Migration chain: `f8b3d1c6a2e9` → `a2b6d4f8c3e1`.
  *Accept:* Admin generates date-range access report → downloads as CSV + PDF with correct rows + hash in AuditLog; same for backup-evidence + access-review; Read-only gets 403; hash in response header matches SHA-256 of body.

## Phase 5 — AI
5.1 AI Agents module (register Claude Code etc.: command template, working-dir scope, read-only mode, pre-change backup, allowed servers; session in terminal; diff review → apply/rollback) · 5.2 Panel copilot ("Ask AI to analyze" on failed jobs; NL actions in palette).

## Phase 6 — Product polish
6.1 Tool installer module (stack components vs version matrix) · 6.2 Reports suite + scheduled email delivery · 6.3 Platform self-backup + master-key escrow runbook · 6.4 First-run installation wizard · 6.5 2FA (TOTP) + recovery codes + session management UI · 6.6 White-label settings surface · 6.7 Drift detection (stretch).

- [x] **6.1 Tool installer module (stack components vs CLAUDE.md version matrix) — backend.** In-code `ToolDefinition` registry (`app/core/tools.py`, never a DB table): id, display name, group (`core stack` / `dev tools` / `AI CLIs`), fixed detect argv + version-parse regex, install action id, needs-root flag. Core stack = git, python3, uv, node, mariadb, redis-server, wkhtmltopdf, bench, nginx, supervisor; dev tools = code-server, gh, htop, jq; AI CLIs = claude-code. `ServerTool` model (`server_tools` table) + migration `b4e7c2a9d1f3` (chains after 6.5 `c3d9a1f5e6b8`; single head; up/down/up on PG18) records server_id, tool_id, detected/recommended version, status `ok|outdated|missing|unknown`, last_checked_at. Recommendation resolver (`app/core/toolinventory.py`) keyed off the highest Frappe version among the server's benches (v14→Py3.10/Node16-18, v15→Py3.11-3.12/Node18-20, v16→Py3.14/Node24; MariaDB 10.6+/11.8; wkhtmltopdf patched-Qt **0.12.6.1** exactly, so a distro `0.12.6` is `outdated`, gotcha #6; missing `uv` is `missing`/critical on **every** bench-bearing server incl. v14/v15, gotcha #2), with Settings→Defaults overrides (B4.17). `server.scan_tools` job extends the 1.2 `check_connection` detection into durable verdict rows; `server.install_tool` per-tool jobs. **Golden rule 1:** one command template per tool — package name / userspace script is a developer-authored constant closed over in the template, the rendered argv has no placeholder assembled from a tool id. Root apt installs use `sudo -n /usr/bin/apt-get install -y <pkg>` mapped 1:1 onto explicit lines in the `docs/implementation-plan.md` sudoers allowlist (absolute paths, one package per line, no wildcard binary, extended in the same commit); wkhtmltopdf routes through a fixed root-owned wrapper `deploy/fdm-wkhtmltopdf` with URL/version/SHA-256 pinned inside it (no caller input — the DOO-220 TOCTOU lesson). Userspace installs (uv, nvm/node, `uv tool install` bench, gh tarball, code-server, claude-code) run as the bench-owner SSH user with **no sudo**. Python + MariaDB are detect-only by design (OS/data-migration decisions, no install template). Every install template is `idempotent=False` (engine never auto-retries a half-finished package install — DOO-1034 `JobFailedNoRetry` precedent) and shares `lock_class="tools"` so two installs on one server serialise (second → HTTP 409). API: `GET /api/servers/{id}/tools`, `POST /api/servers/{id}/tools/scan`, `POST /api/servers/{id}/tools/{tool_id}/install` (202+job_id). RBAC: install → `server:manage` (Admin/Developer), scan → new `tool:scan` (Admin/Developer/Operator), view → `read`; Read-only never mutates (rule 7). Node major resolved server-side from the matrix (enum-bounded), never from the request body. 72 new pytest / 856 full suite green / ruff clean / migration up/down/up on embedded PG18, single head. **Frontend sibling (DOO-1052) landed on main (`709d612`):** `/tools` page + Packages/Tools tab on server detail (B4.13) — grouped checklist with per-row `StatusDot` + detected/recommended chips + action button, group "n of m current" headers, `EmptyState` pre-scan, install → `ConfirmModal` → job detail; `frontend/src/api/tools.ts` typed layer over the backend `GET/scan/install` routes; vite build clean. Root-needing installs are mocked-SSH tested only on the no-sudo live target (gap waived under DOO-342 Option 3 / DOO-333, accepted by M6 gate DOO-264).
  *Accept:* scan a server → each tool shows detected vs recommended version with correct ok/outdated/missing against the server's Frappe-version matrix row (distro wkhtmltopdf 0.12.6 → outdated; missing uv → missing); a userspace install (gh/claude-code) runs end-to-end into `$HOME` with live logs and an audit row; a second install on the same server while one runs gets 409; a root install renders exactly the allowlisted argv; Read-only gets 403 on scan/install, Operator 403 on install.
- [x] **6.6 White-label settings surface (brand token layer).** Extended `PlatformSettings` with brand columns: `logo_dark_path`, `favicon_path`, `accent_hex`, `support_link`, `footer_line`. Migration `b3c4d5e6f7a8` (single head, chains after 5.2 `a1d2c3b4e5f6`; up/down/up on PG + SQLite). `GET /api/branding` unauthenticated-safe endpoint returns only the public brand bundle (product name, logos, favicon, accent, support link, footer); no sensitive settings leak. Upload routes for light logo, dark logo, and favicon — validated server-side by magic-bytes MIME type (not extension), size-capped (logo 512 KB, favicon 64 KB), atomically written under `uploads_dir/branding/`, served with `default-src 'none'` CSP and `X-Content-Type-Options: nosniff`. `--brand-accent` CSS variable added to `:root` in `index.css`; settings store applies it at runtime via `document.documentElement.style.setProperty`; status colours (`--status-ok/warn/err/info`) never touched (uiux-spec B1.5 monochrome discipline). Login page fetches `/api/branding` pre-auth and renders branded wordmark, logo (theme-aware dark/light fallback), and optional support link. Sidebar shows dark/light logo with wordmark fallback on error + optional footer line. Settings → General extended with favicon upload, dark-logo upload, accent colour picker, support link, and footer line fields. Notification emails rebranded with product name in minimal HTML template (dark-mode–native, no external resources). 17 new pytest (all 17 settings tests green) / ruff clean / vite build clean.
  *Accept:* set product name, both logos, favicon, accent, support link in Settings → login page, tab title, sidebar, and notification email all reflect the new brand without restart; `GET /api/branding` while logged out returns only the brand bundle; status colours unchanged.
- [x] **6.7 Config drift detection (uiux-spec A2.16).** `ConfigBaseline` model + migration `c9d4e7a2f1b8`; secret-stripped canonical hashing (`app/core/drift.py`, golden rule 6 — `encryption_key`/db-password/API-key values masked to `••••` *before* hashing, never stored/displayed/logged/notified). Tracked set: per-server nginx.conf / supervisord.conf+conf.d / sudoers.d/fdm-platform / **absence** of the 2.5 elevation drop-in (root-owned, reduced-fidelity via `sudo -n` read allowlist, mocked-SSH tested — DOO-128); per-bench common_site_config.json + platform vhost dir; per-site site_config.json. JobRunner baseline-capture hook fires after any `writes_config=[...]` job (managed change *moves* the baseline — no false positive); `server.drift_check` job (low queue, schedulable via 2.1 `Schedule`, server target) re-hashes + diffs, flags `drifted`, fires `config.drift` notification (2.8) and the Dashboard "Needs attention" row (B4.1). Admin **Accept as new baseline** (audited, reason-required) + **View drift** (sanitised unified diff; hash-only for secret-bearing/unparseable). Read-only on the managed server — never writes/reloads/reverts. Backend green (drift 14/14), ruff clean, migration up/down/up on PG. Frontend delegated to a Frontend/Portal Developer sub-child; ISO 27001 + TA review children gate close (DOO-260).

- [x] **6.2 Reports suite (7 reports) + PDF/CSV + scheduled email (backend).** In-code report registry (`app/core/reports/`): seven declared reports — `fleet_summary`, `backup_evidence`, `job_history`, `app_versions`, `ssl_expiry`, `user_activity`, `server_capacity` — each a `ReportDef` (id, title, required permission, typed+validated param schema, pure `(db, params, *, now) -> ReportResult` generator). Generators query only existing models and **import** every metric from its owner (`dashboard`, `compliance`, `uptime`) — no report defines a number, so a report can never drift from the tile showing the same figure. Renderers are pure-Python: streaming CSV + reportlab PDF (no wkhtmltopdf shell-out, no cairo/pango system deps); both emit the ISO A1.6 evidence header (generated-at / generated-by / explicit range / row-count) for `evidence=True` reports, and CSV data cells are formula-injection–neutralised (CWE-1236). `ReportRun` model + migration `a3f1c2b4d5e6` (chains after current head `dbaa1a7ed7d6`; single head; up/down/up on PG18) records every generation (sha256, byte size, row count, requesting user, window); artifacts land in `settings.reports_dir` with a retention sweep. `report.generate` is a platform-local `CommandTemplate` (`local=True` → `LocalExecutor`, `server_id` NULL) so a run is a real locked/audited/streamed `CommandJob` (rules 2/3); a `Schedule` with `target_type='report'` fires it through the 2.1 scheduler (params carry report id/format/recipients/range) and the 2.8 SMTP sender emails the artifact (timezone default `Asia/Dubai`). API: `GET /api/reports` (catalogue, filtered to the role — sensitive reports hidden), `POST /api/reports/{id}/run` (202+job_id async; `?sync=true` small-CSV inline), `GET /api/report-runs`, `GET /api/report-runs/{id}/download`. RBAC per-report (rule 7): `backup_evidence` + `user_activity` require `report:sensitive` (Admin-only) → Read-only gets 403; all others gated on `read`. `job_history` renders masked params only (rule 6). reportlab pinned in `pyproject.toml`. 28 new pytest / full suite green / ruff clean. Frontend + live scheduled-email E2E (SMTP catcher) are follow-ups. TA review: DOO-35d4afee.
- [x] **6.5 TOTP 2FA + recovery codes + session management.** `UserTOTP` (Fernet-encrypted secret, `confirmed_at`, `last_used_step` replay guard), `RecoveryCode` (sha256-hashed, `used_at`), `UserSession` (one row per login/refresh chain — its `jti` becomes the `sid` claim shared by the access+refresh JWTs), `LoginAttempt` (persisted password/MFA attempt log; 1.1's `LoginThrottle` never wrote these), `SecurityPolicy` (one-row Admin policy: enforce-2FA-per-role, password length/complexity/reuse, IP allowlist, session idle/absolute timeouts) + migration `c3d9a1f5e6b8` (chains after `e2f4a6c8d1b3`; up/down/up on PG). `pyotp`-based RFC 6238 TOTP, 30s step, ±1 window, replay-rejected via `last_used_step`. Login is two-step once TOTP is confirmed: password → `mfa_pending` cookie/token (distinct type, never accepted by `get_current_user`, so it satisfies no RBAC dependency) → `POST /api/auth/2fa/verify` (TOTP or a recovery code) → real session. `GET/POST /api/auth/sessions*` enumerate/revoke/revoke-others, checked on **every** request via `get_current_user` (not just next login) alongside the policy's idle/absolute timeouts. Enforce-2FA-for-role gate blocks a non-enrolled member of an enforced role from any mutating route (`X-Error-Code: mfa_enrollment_required` header) while leaving enrolment/logout reachable. MFA code attempts share the 1.1 `LoginThrottle` mechanism (6 wrong → lockout). `python -m app.manage disable-2fa --email` is the documented, audited host-CLI recovery path for an Admin locked out of TOTP. Every enrol/disable/session-revoke/policy-change writes an `AuditLog` row. 33 new pytest (2fa flow, replay, recovery codes, session revoke-stops-next-request, enforce-2fa block, lockout, IP allowlist, password-policy validator, audit rows) / ruff clean. Frontend (Governance → Security page + two-step login screen, DOO-410) built: active-sessions table with revoke + "this device" marker, 2FA enrolment card with QR and one-time recovery-code panel (copy/download), failed-login log with lockout state, password-policy and IP-allowlist editors. Reviews all PASS — TA backend (DOO-411), TA frontend (DOO-421), ISO 27001 (DOO-414); non-blocking follow-ups tracked in DOO-417/DOO-418/DOO-423. 779 pytest green / ruff clean / vite build clean / migration up-down-up on PG18, single head.
  *Accept:* enrol with a real authenticator app, log out, log back in with a code; replaying the code fails; a recovery code works once; an active session revoked from another device is rejected on its very next request, not just at next login; a policy-enforced Admin without 2FA is blocked from mutating and routed to enrolment; 6 wrong codes lock out; audit rows exist for enrol/disable/revoke/policy-change.
